// This is a list of URLs that are used in the app. It is used to check if the current URL is one of these URLs. If it is, the app will redirect the user to the home page if the user is logged in.
export const URLs = [
    "/auth/signin",
    "/auth/signup",
    "/forgot-password",
    "/reset-password",
    "/"
  ];
  